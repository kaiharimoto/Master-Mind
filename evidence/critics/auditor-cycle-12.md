# The Auditor — cycle 12

*Written here verbatim, as received, before any response was made to it.*
*Categories: 03 One model and sacred positions (20, min 17) · 04 Evidence and integrity (15, min 13). Both are hard gates.*

**Both gates clear — 18 and 13 — and the cycle is declared NOT regression-free.**
The finding is the most expensive one an evidence machine can carry, and it is
mine: artifact 12 prints `everyNodeUnoccludedByChrome` as passing while a node
in its own frame is entirely painted over by the framing-summary strip this
cycle moved into the canvas. The same node is covered in both panels of artifact
08. Four panel instances of one thought, hidden by chrome, with a claim saying
otherwise. Recorded before the response, unedited.

*Recovered from the subagent's own transcript rather than retyped.*

---

I have completed my measurements. Here is my verdict.

---

# The Auditor — Cycle 12

I read only the frozen evidence sets, their ledgers, `MANIFEST.json`, `DIFF.json`, `positions.json`, and `evidence/cycles/README.md`. Per my instructions I did **not** open `DIRECTION.md`, `report.md`, or any other critic's verdict, so where the decision record might have declared an environment substitution, I have judged the substitution only by what the frame itself declares. Everything numeric below I computed myself with PIL/numpy/scipy, ffprobe/ffmpeg, `sha256sum` and `git`.

## What I measured

**Ledger integrity.** `sha256sum -c cycle-12.sha256` → 28/28 OK, no extras (`find . -type f` = 28). Same for cycle-11 and cycle-10. The frozen cycle-12 directory is clean in `git status`; only the *working* set (`evidence/05, 08, 10, MANIFEST.json`) has moved on since the freeze, which does not touch what I judged.

**Build provenance.** All 20 cycle-12 artifacts carry one `appSha` = `78e03b777fc6b68f`. I verified independently: `git show ad1d073:src/dist/app.js | sha256sum` = `78e03b777fc6b68f73d9…`. The chain artifact → manifest → committed bundle closes exactly. Cycle 11 recorded no per-artifact `appSha` at all.

**Positions.** `positions.json` is byte-identical (`6558507f7990`) across cycles 9, 10, 11 and 12 — 150 nodes on map-fermentation, 11 on map-talk, zero moved, zero added, zero removed under a coordinate-by-coordinate diff.

**Cross-cycle marker displacement** (blob-centroid matching, cycle-11 → cycle-12): 02 = 80/80 matched, max 0.09 px · 03 = 118/118, max 0.21 px · 04 = 86 matched, max 0.36 px (+4 markers newly *revealed*) · 06 = 8/8, 0.08 px · 07 = 7/7, 0.00 px · 08 = 13 matched, 0.24 px · 09 = 27/27, 0.00 px · 10 = 9/9, 0.00 px · 11 = 12/12, 0.00 px · 13 = 6/6 · 14 = 50/50, 0.00 px.

**Twin agreement, within cycle 12.** Artifact 11: best horizontal alignment of the canvas band (rows 350–830) is at exactly 960 px with mean absolute difference **0.0** — the panels differ only in rows 10–51, 124–191 and 1028–1069 (header, toolbar, footer badge), i.e. the chrome. Artifact 12: Windows/Android markers coincide at **0.00 px** on the 11-node map (5 markers) and **0.00 px** on the 150-node map (62 markers); the only extra blobs on the Android side are the glyphs of its "Drag a node — Place / move" badge.

**Runtime stillness.** Video 19, idle window 0.2–3.5 s at 6 fps: 28 markers, centroid drift **0.000 px** across all 20 frames. Video 19 post-placement tail 21.3–22.9 s: 17 markers, ≤0.43 px on one frame (codec noise). Video 17 idle tail 33.6–35.5 s: **117 markers, 0.000 px**. Nothing drifts, wobbles or settles.

**AR vantage claim, checked with my own ruler.** Panel offset is exactly 1280 px (the two AR-tab highlights land at x=302 and x=1582). The "Sauerkraut by weight" highlight ring is at (275.5, 533.3) in panel 1 and (1677.2, 307.0) in panel 2 → panel-relative Δ = (121.7, −226.3) = **256.9 px** against the frame's stated 252 px. ~2% — the vantage moved, the node did not.

**DIFF honesty.** I recomputed SSIM for five artifacts: 02 = 0.9330 (DIFF: 0.928), 04 = 0.9222 (0.916), 12 = 0.8231 (0.814), 08 = 0.9465 (0.9430), 13 = 0.9855 (0.9846). Honest to ≤0.007 with a different window function. `DIFF.json` flags, unprompted, all seven recipe changes as "not a like-for-like recapture", the one removed claim, every added claim, every sub-threshold SSIM, and artifact 01's byte-identity.

**Contact sheets are real.** Sheet 20's last panel is labelled 37.9 s; I extracted the video frame at 37.9 s and matched it into the sheet at scale 0.5, offset (6, 5298), MAD **0.48**. Not stale, not staged.

**The double freeze, checked in git.** First freeze `d4b6d4c` = 15/20 (03, 12, 17 `claim-not-met`; 05, 14 `driver-error` on headline overflow). Re-freeze `ad1d073` = 20/20. Comparing the two manifests: **no claim was removed**; every claim that failed (`travelScaleDescribesTheDrawnPanel`, `clusterDisplacementVisibleInPixels`, `captionNeverOutrunsTheDetector`, `mapReturnedToItsStartingLayout`) still exists and now passes, and `appSha` moved `50b6d8a4` → `78e03b77`. The fixes went into the build, not into the assertions. Artifact 12 was fixed with an *unchanged* capture script (`fnSha` `936d2e118cf8cc23` both runs).

**Video specs.** 16: 1920×1080, 30 fps, 34 s (min 30) · 17: 41 s (min 24) · 18: 13 s (min 10) · 19: 23 s (min 15) · 20: 41 s (min 20, up from 30 s). All pass.

## What I found against the build

A node is being painted over by new in-canvas chrome. In cycle 11, artifact 08 has a node marker at (503, 488) and (1463, 488) — colour (152, 57, 31), one node drawn twice in the before/after pair. In cycle 12 those exact pixels read (25, 20, 16), the background of a new strip occupying (486, 483, 440×26) and (1445, 483, 441×27). The same node is covered again in artifact 12's two top panels, under strips at (485, 540, 398×24) and (1445, 540, 398×24). The map seed sha is `ca06ee0d6d5b` in both cycles and every neighbouring marker matches to ≤0.24 px, so the node was not deleted and did not move — it is hidden. Four panel instances of one node, in two artifacts.

That matters twice over, because artifact 12's manifest asserts `everyNodeUnoccludedByChrome` = true, pass. I can read the frame; the claim does not hold for it.

And artifact 12's 150-node panel is the one artifact in my regression set I cannot actually regress: its printed ledger sha moved `d05073a5ff` → `8a9ee216fc` between cycles, and only 10 of 53 markers align under any single translation I searched (±40 px). The committed ledger is untouched and the frames say the cluster is displaced mid-drag, so this is explicable — but `DIFF.json`'s positions block reports only "identical: true" for the committed ledger and says "inspect", never naming the on-frame sha move.

```json
{
  "critic": "auditor",
  "cycle": 12,
  "regressionFree": false,
  "positionRegression": false,
  "categories": [
    { "id": "03", "name": "One model and sacred positions", "weight": 20, "minimum": 17, "score": 18,
      "reasoning": "Every measurable form of this category holds. The committed ledger (positions.json) is byte-identical across cycles 9-12 — 161 nodes, zero moved, zero added, zero dropped. The twin pair is the strongest it has been: in artifact 11 the canvas band aligns between the Windows and Android panels at exactly 960 px with mean absolute difference 0.0, differing only in rows 10-51/124-191/1028-1069 which are header, toolbar and footer chrome; in artifact 12 the two surfaces agree to 0.00 px on 5 markers of the 11-node map and on 62 markers of the 150-node map after a cross-surface drag and a concurrent conflicting edit, with no node dropped. Cross-cycle, artifacts 02/03/04/11 hold to 0.09/0.21/0.36/0.00 px, and artifact 04's four 'extra' markers are four nodes a 'Koji-kin sourcing' label chip covered in cycle 11, not four new nodes. Runtime stillness is real and not merely asserted: 28 markers hold to 0.000 px over 3.3 s of idle in video 19 and 117 markers hold to 0.000 px over 1.8 s in the hand-tracking video, so no lens nudges a placed node. I verified artifact 03's headline myself — the highlight rings give 256.9 px of on-image travel against the 252 px the frame claims, at a panel offset of exactly 1280 px, so the vantage moved and the node did not. Two points come off. The lens now hides a node that the lens used to show: the same map-talk node is fully painted over by a new in-canvas 'labels hidden / listed at right' strip in four panels across artifacts 08 and 12, and for a category whose story is that you can see the thought where you left it, a node behind chrome is a lens failure even when the coordinate is perfect. And the twin's independence remains unfalsifiable from the pixels — identical rasters are the correct outcome for one deterministic SwiftShader scene, and the differing chrome shows the panels are separate renders, but the three new claims that would settle it (twoDistinctSockets, panelRuntimesDiffer, eachPanelNamesItsRasteriser) print their rule as the literal string 'true', so a critic can see the assertion and not the measurement." },
    { "id": "04", "name": "Evidence and report integrity", "weight": 15, "minimum": 13, "score": 13,
      "reasoning": "The machinery is the best I could test and it survived every test but one. The ledger verifies 28/28 with no extras; all 20 artifacts are present at their defined dimensions and every video exceeds its minimum duration and frame rate; all 20 share one appSha which I independently confirmed equals sha256 of src/dist/app.js at the freeze commit ad1d073, closing the artifact-to-build chain that cycle 11 could not close at all. DIFF.json incriminates itself rather than flattering the cycle: it names all seven recipe changes as 'not a like-for-like recapture', names the one removed claim on artifact 17 as 'a machine-checked assertion that used to hold is no longer being made', lists every sub-threshold SSIM, and explains artifact 01's byte-identity with its recapture timestamp; I recomputed five of its SSIMs and they are honest to 0.007. The double freeze is disclosed beside the sets and legible in git, and — the thing that mattered most — comparing the discarded 15/20 manifest against the shipped one shows no claim was removed and every claim that failed still exists and now passes against a changed build. I even confirmed the contact sheets are derived from the videos they claim (sheet 20's 37.9 s panel matches the extracted frame at MAD 0.48). Against all that stands one fact that this category exists to catch: artifact 12 prints everyNodeUnoccludedByChrome as passing while a node in its own frame is fully covered by a chrome strip. A false passing claim is the most expensive defect an evidence machine can have, because it is the thing critics are asked to trust in place of looking. Two smaller marks: the frozen MANIFEST.json presents a clean captured:20 with lateFaults:[] and carries no trace of the re-freeze, so the disclosure lives only in README.md and git; and most claims print rule 'true' (ruleSha b5bea41b6c62, the sha of the string 'true'), which exposes the assertion but not the measurement." }
  ],
  "findings": [
    { "artifact": "12", "severity": "major", "subsystem": "evidence",
      "observed": "MANIFEST.json records claim everyNodeUnoccludedByChrome with actual:true, pass:true. In the shipped frame the node that sits at (502,545) in the Windows panel and (1462,545) in the Android panel — colour (152,58,31) in cycle 11, with three filaments radiating from it — reads (25,20,16) in cycle 12, the background of the 'N label hidden / listed at right' strip occupying (485,540,398x24) and (1445,540,398x24). The map seed sha is ca06ee0d6d5b in both cycles and every neighbouring marker matches to 0.00 px, so the node is present and unmoved in the model and invisible in the frame.",
      "rootCause": "The occlusion predicate is almost certainly evaluated against the bottom 150-node panel block only (it sits among bigMapShownOnBothSurfaces and bigMapLedgersIdentical), or against label chips rather than the framing-summary strip, so the strip that was moved into the canvas this cycle is not in the set of rectangles it tests.",
      "correction": "Evaluate the predicate over every drawn node in every panel of the composite against every chrome rectangle including the framing-summary strip, and let it fail the capture. Then keep the node: either pin that strip outside the drawn extent or let it displace, as artifact 11's version of the same strip does at (569,175) above the constellation." },
    { "artifact": "08", "severity": "major", "subsystem": "canvas",
      "observed": "The same map-talk node is covered in both panels of the before/after composite. Cycle 11 has a filled marker at (503,488) and (1463,488) with pixel value (152,57,31); cycle 12 reads (25,20,16) at both, inside strips at (486,483,440x26) and (1445,483,441x27) whose background runs rows 484-507. The 13 other coloured markers match cycle 11 to within 0.24 px, so this is occlusion, not a move or a deletion.",
      "rootCause": "The framing-summary strip is laid out at a fixed offset inside the canvas rather than in the gutter, and its placement is not tested against node positions before it is drawn.",
      "correction": "Give the strip the same treatment the label arbiter already gives labels — it should be placed where it covers no marker, or drawn behind the node layer so markers punch through it." },
    { "artifact": "12", "severity": "minor", "subsystem": "evidence",
      "observed": "The bottom 150-node panels print pos sha 8a9ee216fc where cycle 11's printed d05073a5ff, and the constellation does not align cycle-over-cycle: at best 10 of 53 detected markers coincide under any translation in a +/-40 px search. DIFF.json rows['12'] reports ssim 0.814 and 'inspect', and its positions block reports identical:true — which is true of the committed ledger and says so — but nothing names the on-frame sha move.",
      "rootCause": "The frame captures a transient mid-drag displaced state whose magnitude changed when clusterDisplacementVisibleInPixels was added, while the diff's position check reads only the post-writeback committed ledger.",
      "correction": "Have DIFF.json read the pos sha printed on each panel and diff those alongside the committed ledger, so a moved transient state is named as a moved transient state rather than left inside a generic 'inspect'." },
    { "artifact": "12", "severity": "minor", "subsystem": "evidence",
      "observed": "The frozen MANIFEST.json reads cycle:12, captured:20/20, allCapturedInThisCycle:true, staleFromEarlierCycles:[], lateFaults:[]. Git shows the same cycle number was frozen at d4b6d4c with 15/20 — 03/12/17 claim-not-met and 05/14 driver-error — before ad1d073 replaced it. Nothing in the frozen manifest records that.",
      "rootCause": "--refreeze rewrites the manifest from the successful run rather than appending the superseded run's outcome to it.",
      "correction": "Carry a supersededRuns array in the manifest naming each earlier run of the same cycle number, its captured count, and the ids and reasons that failed. The README disclosure is good; it should not be the only place a critic reading the frozen set can find it." },
    { "artifact": "12", "severity": "minor", "subsystem": "evidence",
      "observed": "Cycle 12 added rule text and ruleSha to every claim, which cycle 11 lacked entirely. In practice 16 of artifact 12's 17 claims print rule 'true' with ruleSha b5bea41b6c62 — the sha256 of the four characters 'true' — and the pattern repeats across the set. The one claim I could independently test against its own frame, everyNodeUnoccludedByChrome, is false.",
      "rootCause": "The fingerprint is taken over the assertion applied to the computed value, not over the code that computes the value, so a boolean claim fingerprints as the constant true.",
      "correction": "Fingerprint the predicate function that produces `actual`, not the assertion applied to it, and print the measured quantity beside the boolean (as artifact 12's own caption already does for the 61.76 px of cluster travel) so a reader can check the number rather than the verdict." },
    { "artifact": "01", "severity": "minor", "subsystem": "evidence",
      "observed": "01_maps_home.png is byte-identical to cycle 11 (sha 2c8ae64ea1ff both cycles). The frame itself is correct — both seeded maps at 150 and 11 nodes, last-opened times, and create/rename/delete each shown operating. The only evidence that it was recaptured rather than copied forward is the manifest's own fileMtime 2026-09-06T09:17:22.091Z and capturedInThisRun flag, both self-reported by the process under audit.",
      "rootCause": "Deterministic seeding (epoch 1787227200000) makes this frame reproducible to the byte, which is a virtue; but the attestation of recapture is not independent of the recapturer.",
      "correction": "Have the capture stamp a per-run nonce into a region the claims already ignore, or record the run id in the ledger line, so byte-identity and freshness can both be checked by a reader who trusts nothing in the manifest." }
  ],
  "comparisons": [
    { "artifact": "01", "verdict": "unchanged", "why": "Byte-identical, sha 2c8ae64ea1ff in both cycles; DIFF flags it and attributes it to determinism. Frame still satisfies its definition: 150 and 11 node counts, last-opened times, create/rename/delete all shown." },
    { "artifact": "02", "verdict": "improved", "why": "80/80 markers matched at max 0.09 px, so positions are untouched; the framing summary goes from '109 labels hidden, 16 shortened' to '81 hidden, 10 shortened, 37 named by their first letters', with the 81 unnamed thoughts listed by district at the right. 28 fewer anonymous nodes." },
    { "artifact": "03", "verdict": "improved", "why": "118/118 markers matched at max 0.21 px. Adds travelScaleDescribesTheDrawnPanel, and the panel now states its drawn scale; I measured the stated 252 px of vantage travel at 256.9 px from the two highlight rings at a panel offset of exactly 1280 px." },
    { "artifact": "04", "verdict": "improved", "why": "86 markers matched at max 0.36 px and four more markers are visible in cycle 12 — at (1039,390), (1092,391), (1152,399), (1040,405) — because the 'Koji-kin sourcing' label chip that covered them in cycle 11 has moved. Two chrome-overlap claims added." },
    { "artifact": "05", "verdict": "improved", "why": "Recipe changed and the first cycle-12 run's driver-error on headline width is fixed; five claims where cycle 11 had four, including chipPromisesNoLegibilityItCannotDeliver. Only 12 of ~170 blobs are common across cycles, as expected for a different webcam frame; I did not grade its content, which is outside my categories." },
    { "artifact": "06", "verdict": "unchanged", "why": "8/8 markers matched at 0.08 px; two chrome-overlap claims added, no other measurable movement." },
    { "artifact": "07", "verdict": "unchanged", "why": "7/7 markers matched at 0.00 px; two chrome-overlap claims added." },
    { "artifact": "08", "verdict": "regressed", "why": "A node marker present in cycle 11 at (503,488) and (1463,488), pixel (152,57,31), reads (25,20,16) in cycle 12 — the background of new in-canvas strips at (486,483,440x26) and (1445,483,441x27). Same seed sha ca06ee0d6d5b, 13 other markers matched to 0.24 px: the node is hidden, not moved or deleted." },
    { "artifact": "09", "verdict": "unchanged", "why": "27/27 markers matched at 0.00 px. It carries the same in-canvas strip at (649,492) and (1609,492) but no marker falls under it." },
    { "artifact": "10", "verdict": "unchanged", "why": "9/9 markers matched at 0.00 px; ssim 0.951 accounted for by label and chrome differences." },
    { "artifact": "11", "verdict": "improved", "why": "12/12 markers at 0.00 px and the canvas band is pixel-identical between panels at exactly 960 px offset while header, toolbar and footer differ. Claims go from one to four, adding twoDistinctSockets, panelRuntimesDiffer and eachPanelNamesItsRasteriser — the right three questions, even if their rule prints as 'true'." },
    { "artifact": "12", "verdict": "regressed", "why": "It gained a great deal — a second panel pair showing the 150-node map on both sockets with the two surfaces agreeing to 0.00 px on 62 markers, plus printedShaMatchesRenderedState and clusterDisplacementVisibleInPixels. But the same map-talk node hidden in artifact 08 is hidden here too, in both top panels, under strips at (485,540) and (1445,540) — while the manifest prints everyNodeUnoccludedByChrome as passing. A claim contradicted by its own frame outweighs the gains for this artifact." },
    { "artifact": "13", "verdict": "unchanged", "why": "6/6 markers at 0.00 px; ssim 0.9855 by my own computation against the DIFF's 0.9846." },
    { "artifact": "14", "verdict": "improved", "why": "50/50 markers at 0.00 px; recipe changed and detailHeadingsFitTheirColumns added after the first cycle-12 run failed this artifact on a 450 px headline in a 446 px column." },
    { "artifact": "15", "verdict": "unchanged", "why": "No markers to compare; ssim 0.981, below its 0.995 threshold, which DIFF flags as 'the lit subject moved — inspect'. Nothing in it bears on positions or on set integrity." },
    { "artifact": "16", "verdict": "improved", "why": "34 s at 1920x1080/30fps against a 30 s minimum; two claims added (devicePortraitAsInTheHero, marginReferenceFitsItsColumn) and the recipe changed. Frame-to-frame profile shows 33 of 67 sampled intervals below 0.05 mean absolute difference — real still windows, not constant churn." },
    { "artifact": "17", "verdict": "improved", "why": "41 s against a 24 s minimum. clusterMoved was removed and replaced by six sharper claims (clusterMovedByPose, clusterMovedByMouse, clusterArrangementPreservedByPose, mapReturnedToItsStartingLayout, undoStackEmptiedOnCamera, captionNeverOutrunsTheDetector), and DIFF flags the removal against itself. In its idle tail I measured 117 markers holding to 0.000 px over 1.8 s." },
    { "artifact": "18", "verdict": "unchanged", "why": "13 s against a 10 s minimum, same specs; ssim 0.9243 just under its 0.94 threshold, consistent with a re-encode and label changes rather than a different take." },
    { "artifact": "19", "verdict": "unchanged", "why": "23 s against a 15 s minimum, same six claims. Its idle windows are the cleanest stillness evidence in the set: 28 markers at 0.000 px across 20 frames spanning 3.3 s, and 17 markers at 0.43 px worst case after the node is dropped." },
    { "artifact": "20", "verdict": "improved", "why": "30 s to 41 s against a 20 s minimum, three claims added including clipboardCarriesTheExportedPrompt and roundTripShownAt150Nodes. I confirmed its contact sheet is honest: the 37.9 s panel matches the extracted video frame at scale 0.5, MAD 0.48." }
  ],
  "verdict": "This cycle is not regression-free, and the regression is narrow and specific: a new in-canvas framing-summary strip now paints over a map-talk node that cycle 11 showed, in four panels across artifacts 08 and 12, which I confirmed by reading (25,20,16) strip background where cycle 11 read (152,57,31) node while every neighbouring marker held to within 0.24 px and the seed sha ca06ee0d6d5b was unchanged. In artifact 12 that occlusion also falsifies a claim the same cycle added and printed as passing, everyNodeUnoccludedByChrome, which is the one thing an evidence machine must never do. There is no position regression. The committed ledger has been byte-identical since cycle 9, every comparable artifact holds its markers to at most 0.36 px cycle-over-cycle, the two surfaces agree to 0.00 px on both maps in the twin composites, and in the idle windows of the videos 28 and 117 markers respectively hold to 0.000 px — nothing tidies, drifts or wobbles. Set against the one bad claim, the rest of the machinery is the strongest I could test: the ledger verifies 28/28, the manifest's appSha resolves exactly to the committed bundle at the freeze commit, DIFF.json's SSIMs match mine to 0.007 and it reports its own recipe changes and its own removed claim without being asked, the contact sheets are demonstrably the videos they claim, and the re-freeze of this cycle number — properly disclosed beside the sets and legible in git — removed not one assertion, fixing five failures in the build instead. That last point is what keeps category 04 above its minimum despite the false claim. Both categories clear their gates: 03 at 18/20, 04 at 13/15."
}
```