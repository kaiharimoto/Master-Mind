# The Auditor — cycle 10

*Written here verbatim, as received, before any response was made to it.*
*Categories: 03 One model and sacred positions (20, min 17) · 04 Evidence and report integrity (15, min 13). Both are hard gates.*

**Category 04 scores 12 against a minimum of 13 — the second cycle running that
this gate has failed — on two blocking findings, and cycle 10 is declared NOT
regression-free.** Recorded before the response, unedited, including that.

*Recovered from the subagent's own transcript rather than retyped.*

---

## Scores

| Category | Score | Weight | Minimum | Met? |
|---|---:|---:|---:|---|
| 03 — One model and sacred positions | 17 | 20 | 17 | Yes (at the line) |
| 04 — Evidence and report integrity | 12 | 15 | 13 | **No** |
| **Total (my two categories)** | **29** | **35** | **30** | **No** |

---

## Regression verdict

**Cycle 10 is NOT regression-free against cycle 9.** Three artifacts regressed.

**R1 — artifact 10 (`10_search_flyto_end.png`).** The search-match-reason chip regressed from fully readable to 84 % occluded. Measured: the chip's rounded border spans x = 1144–1432 (288 px) at y = 49; the newly-added "7 labels hidden · 1 shortened at this zoom" chip's left edge is at x = 1191 (background step measured along row y = 75), leaving 47 px — 16.3 % — visible. Only "19 hit" survives. In cycle 9 the same chip spans x = 1145–1431 with nothing over it and reads in full: "19 hits · 6 in the text · 13 in the label 'Koji'". The MANIFEST claim `searchMatchReasonShown: true` still passes.

**R2 — artifact 04 (`04_mind_expansion.png`).** Same collision, worse: search chip x = 1144–1432, covering chip left edge x = 1169 → 25 px (8.7 %) visible. Additionally the unlabelled-thought enumeration was removed (DIFF's own `unlabelledListed 50 -> 0`; I count 49 rendered swatch rows in cycle 9's sidebar, 0 in cycle 10) while hidden labels rose 87 → 105 — a net legibility loss on the artifact whose definition is "the entire 100+ node map on screen at once and legible".

**R3 — artifact 17 (`17_hand_vocabulary.mp4`).** The machine-checked claim `clusterMovePropagatedToTheOtherSurface` (present and passing in cycle 9's MANIFEST) is gone in cycle 10, with no replacement on any artifact that shows two surfaces, and no mention in DIFF.json. Artifact 17 is single-surface in both cycles (the badge reads "WINDOWS · MIND EXPANSION" in every frame I sampled at t = 27.7, 29.7, 33.8, 34, 36, 38, 40 s in both takes), so cycle 9's claim was itself uncorroborated — but its silent removal is invisible to the diff.

Everything else held or improved. Improved: **02** (label overlaps eliminated — cycle 9 shows visibly collided labels near x≈1050, y≈206 and x≈1250–1300, y≈290–315; shortened count 36 → 16; the 109 unlabelled now enumerated), **06** (reframed onto the holding ring, all 8 held markers countable inside it), **18**/**19** (same label-arbiter rework), **14** (claim rename, see below — an integrity improvement). Unchanged: **01** and **15** byte-identical, **08**, **11**, **12**‑core, **13**, **16**, **20** within threshold.

**Node positions: no node moved.**
- `positions.json` is byte-identical across cycles **2 through 10** (sha `6558507f…`; checked by joining every cycle ledger against cycle-10's).
- Screen-space check, cycle 9 → cycle 10: every saturated node marker in **02** (61/67), **04** (77/79), **07** (8/8), **08** (10/10), **10** (11/11) has a counterpart within 1.0 px; the 2 unmatched in 02 and 04 are markers occluded by differently-chosen labels. **06** moved because its camera was deliberately reframed onto the ring, a declared recipe change — not a node move.
- The ledger cross-checks against the pixels: `s7427082a98 · 0.4, 0.2, -0.7` (artifact 07) = `[0.4, 0.2, -0.7]`; `sec77dd4720 · 2.1, -10.3, 3.9` (artifact 08 "before") = `[2.056, -10.26, 3.869]`; `n3e0d08c741 · 16.5, 12.5, -9.5` (artifacts 04/10) = `[16.511, 12.545, -9.513]`.
- No idle drift found anywhere I measured: artifact 20, 11 markers over t = 5→8 s, max displacement **0.14 px**; artifact 16, 10 markers over t = 2.5→5.5 s, max **0.13 px**; artifact 17, 135 markers over t = 34→35 s, max **0.13 px**.

**Baseline soundness — established, with one limit.** `sha256sum -c` of `cycle-9.sha256` against `cycle-9/` verifies **28/28 OK** with no unlisted files; independently, every `check.sha256` inside `cycle-9/MANIFEST.json` matches the file on disk (0 mismatches), so the set I diffed against is self-corroborating and is the set cycle 9's critics read. Cycle 10 verifies identically (28/28, 0 mismatches). DIFF.json's `capturedPrev: 20` is counted from files actually present (not from the previous manifest) and `headerDisagreement: null` — I confirmed cycle-9's manifest self-reports 20 and 20 artifact files exist. The README's disclosed incidents (the cycle-7 overwrite, the post-freeze cycle-8 DIFF rewrite) do not touch the cycle-9 baseline, which the README says was written at freeze time. **Limit:** I cannot verify the README's git-provenance table without build history I am barred from reading, so "sound" here means internally consistent and hash-verified as it stands, not attested against an outside record.

---

## Blocking findings

**B1 · artifact 10 (and 04) · claim contradicted by its own pixels.** `searchMatchReasonShown: true` passes on artifact 10, but the shipped frame shows only "19 hit" — 83.7 % of the 288 px chip (x 1144–1432) is covered from x = 1191 by the labels-hidden chip. DIFF.json compounds this on artifact 04 by recording, under **`onFrameChanged`**, the value `searchBreakdown null -> "19 hits · 6 in the text · 13 in the label “Koji”"` for a string that is 91.3 % invisible on that frame (covering edge x = 1169). Root cause is visible: the collision occurs only when the node editor is open — in `18_search_flyto.mp4` at t = 1.0 s the editor is closed, the right-aligned chip sits at x ≈ 1580 and the search chip reads in full. **Fix:** give the labels-hidden chip and the search chip separate rows (or stack them) when the editor panel narrows the toolbar, and add a claim that asserts the search reason's *rendered* extent is unoccluded — the way artifact 12 already asserts `everyNodeUnoccludedByChrome`.

**B2 · evidence apparatus · the diff cannot see claims.** DIFF.json compares bytes, SSIM, recipe fingerprints, subject luminance and positions — but not the MANIFEST claim sets. Because of that, artifact 17 lost `clusterMovePropagatedToTheOtherSurface`, artifact 14 renamed `detailIsMagnified` → `detailExceedsPanelScale`, artifact 12 gained `clusterMoveCrossedTheBoundary`, and 02/04/05/06/09 gained claims — and **none of these appear anywhere in the diff**. A machine-checked assertion can therefore be deleted between cycles with no trace, which is precisely the failure mode this category exists to catch. Measured by diffing claim names across the two MANIFESTs: 8 artifacts have added or removed claims; 0 are mentioned in DIFF.json. **Fix:** add a `claims` section to each DIFF row listing added / removed / renamed claim ids, and fail the cycle on a removal that has no stated replacement.

---

## Major findings

**M1 · artifact 12 · `clusterMoveCrossedTheBoundary` asserted on an artifact that shows no cluster.** The four panels of `12_sync_twin_after.png` (1920×2160) are: map-talk twin after an Android drag+edit, and the 150-node map on the two sockets. Nothing in the headers, the canvases, or the editor mentions or shows a cluster; the dragged node `s480af97d09` did not cross the holding boundary either (holding reads 4 in both artifact 11 and artifact 12, and the node sits outside the dashed ring in both). This is the deleted artifact-17 property re-homed onto an artifact that cannot corroborate it, and re-worded in the move (propagation → boundary crossing). **Fix:** put the claim back on artifact 17, where the frames do show a cluster move — at t = 23→24 s under a static camera I measure 11 of 134 detected markers displaced while the other 123 hold to a median of **0.00 px** and p90 **0.13 px**, and the HUD reads "cluster Koji · 16 nodes moved together · travelled 17.51 · internal arrangement drift 0.000000".

**M2 · artifact 12 · `concurrentConflictKeptBoth` only half corroborated.** The header asserts "Android retexted+recoloured 'Demo: search fly-to' while Windows relabelled it — both kept". The frame proves the recolour: that node's mean fill goes from teal (18, 90, 84) in artifact 11 to magenta (141, 42, 92) in artifact 12, at the position the 11→12 similarity transform predicts (residual 0.38 px). It proves neither the retext nor the relabel — the node's label renders shortened as "Demo: search…", and the open editor is on a different node ("Slide budget: 12", label "admin"). **Fix:** open the editor on the conflicted node in one of the two panels so its text field and label field carry the "both kept" result, rather than asserting it only in prose.

**M3 · DIFF.json · `whatChanged` names no change for half the set.** All 10 artifacts in `recipeChangedIds` (02, 03, 04, 05, 06, 07, 09, 10, 14, 17) are explained as "capture script changed (hashA -> hashB); demonstrates now: *X*". I compared the `recipe.demonstrates` strings across the two MANIFESTs: **all 10 are byte-identical to cycle 9's.** The phrase "demonstrates now" implies a change of purpose where there is none, and the actual change — a label-arbiter rework that removed overlaps, added the labels-hidden chip and moved the unlabelled sidebar from 04 to 02 — is never stated. **Fix:** report the delta (`demonstrates: unchanged`) and carry the on-frame counters that did change into the sentence.

**M4 · DIFF.json · classification not self-consistent.** Artifact 19 carries `subjectChanged: true` with ssim 0.9056 against its own 0.93 threshold, yet is `substantive: false` with no `whatChanged`; artifact 12 has the identical shape (subjectChanged true, no recipe change, below threshold) and is `substantive: true` with a `notes` entry. Artifacts 08 (0.968 vs 0.985) and 18 (0.928 vs 0.940) likewise cross their thresholds with no explanation at all. Three artifacts moved past their own tripwires unremarked. (For the record, the SSIM values themselves are honest — I recomputed all 11 comparable PNGs and all 5 contact sheets with a standard gaussian-window SSIM and agree to within 0.020, mean |Δ| ≈ 0.008, with identical ordering.) **Fix:** make "below threshold" always produce a `notes` entry, and derive `substantive` from one stated rule.

**M5 · artifact 04 · legibility regression, disclosed but not owned.** 87 → 105 labels hidden and the 49-row unlabelled sidebar removed, on the artifact defined as "the entire 100+ node map on screen at once and legible". DIFF records the numbers under `onFrameChanged` but classifies the row only as "capture script changed"; nothing says the frame now names fewer of its thoughts than it did. **Fix:** restore the enumeration alongside the node editor, or write the trade-off up as a finding.

---

## Minor findings

**m1 · artifacts 11/12 · the twin panels do not render identical labels.** Node markers are identical to the pixel — in artifact 11 the whole canvas region (x 0–960 vs 960–1920, y 200–1000) differs by a maximum absolute channel-sum of **0**; in artifact 12's 150-node row all **49** detectable coloured markers share sub-pixel centroids with zero unique to either side. But 8 of 48 detected label runs differ in position or width between the two panels (both sides report 116 hidden; the frames disclose "17 shortened" vs "19 shortened"). "Framed identically… node-for-node identical" therefore reads as partly untrue at a glance. Fix: seed the label arbiter identically per surface, or state on-frame that label selection is per-surface.

**m2 · DIFF.json · `unlabelledListed` exceeds what the frame renders.** Counting connected components in the sidebar swatch column (x 1630–1660, full frame height): cycle-10 artifact 02 renders **47** rows plus an "…and 62 more" line (47 + 62 = 109, matching "109 labels hidden"), while DIFF reports 48; cycle-9 artifact 04 renders **49**, DIFF reports 50. A model count presented under `onFrameChanged`.

**m3 · artifact 03 · holding cluster is out of frame.** The brief's definition asks for it in frame. In the left panel only the top arc of the dashed ring and one truncated label ("Black garlic: 60C for 40…") are inside the 1280×1440 device frame; the held markers are clipped at the bottom edge and partly behind the gyro HUD chip. The recipe's `demonstrates` no longer mentions holding, so the narrowing is implicit rather than declared.

**m4 · artifact 14 · detail headline clipped mid-word.** "Detail ×1.1 of app pixels — the pan" is cut at x ≈ 485 by the next column. Present in cycle 9 too ("— t"), so unchanged, not a regression.

**m5 · positions.json proves less than its name implies.** It holds `s480af97d09 = [-6.1, -4.3, -3.1]` — artifact 12's *pre-drag* value — and no node at artifact 17's `n69344565b8 · 4.4, -8.8, 18.0` (the ledger has `[-8.453, 2.442, 20.05]`; that node was moved by the pose-driven cluster grab I measured at t ≈ 23.5 s, an explicit act). So the file is the committed-seed ledger, and its nine-cycle constancy evidences seed stability, not runtime stability. The real no-drift evidence is in the pixels, and it is good — but the diff's `positions.identical: true` should say what it is checking.

---

## What is strongest

The sacred-positions claim is proven at the pixel, not asserted: in artifact 11 the Windows and Android canvases differ by exactly zero across 768,000 pixels while their headers name different runtimes and sockets; between artifacts 11 and 12 every node maps under a single uniform similarity (scale 0.9556, residual ≤ 1.35 px, ≤ 0.40 px for the five coloured nodes) with exactly one node — "Slide budget: 12" at (413, 589) — vacating its place, matching the declared Android drag and the pos-sha change 198501d063 → f9a4912f50. Artifact 03's headline number survives an independent check: the stated "271 px travel in the device's own 1280×1440 frame (125, −240)" reconstructs to 268.5 device px from my measured composite travel of (117, −221) once the panel's device→composite scale of 0.931 is recovered independently from both quoted x values (giving a consistent 44.5 / 45.1 px inset). And the evidence apparatus is genuinely auditable rather than self-certifying — the external ledger verifies 28/28 on both cycles, every MANIFEST digest matches its file, the previous cycle's artifact count is taken from files present rather than from its own manifest, environment substitutions are stamped into the pixels ("SYNTHESISED CAPTURE · … · REAL HANDLANDMARKER"; "no camera pass-through"), and cycle 10 renamed `detailIsMagnified` to `detailExceedsPanelScale` — correcting a claim whose name did not match what it checked, since three of the four detail panels are below 1× of app pixels in both cycles.

---

```json
{
  "critic": "auditor",
  "cycle": 10,
  "regressionFree": false,
  "positionRegression": false,
  "categories": [
    { "id": "03", "name": "One model and sacred positions", "weight": 20, "minimum": 17, "score": 17,
      "reasoning": "The substance is proven at the pixel: artifact 11's two panels differ by zero over the whole canvas region; artifact 12's 150-node row has all 49 detectable markers at identical sub-pixel centroids with only 8 of 48 label runs differing; 11->12 is a single uniform similarity (s=0.9556, residual <=1.35px) with exactly one node vacating its place, matching the declared drag; artifact 08 shows 8 of 10 markers within 1px across a frozen camera with only the placed node moving and holding 4->3; artifact 05's open-palm move is a pure dolly (uniform scale 1.136 vs the 1.1459 implied by the declared view distances); artifact 17 moves 11 of 134 markers under a static camera while 123 hold to median 0.00px with an on-frame drift readout of 0.000000; and no idle drift exceeds 0.14px anywhere I measured. Deductions: artifact 12 carries clusterMoveCrossedTheBoundary with no cluster anywhere in its 1920x2160 frame, and concurrentConflictKeptBoth is only half corroborated (the recolour teal(18,90,84)->magenta(141,42,92) is visible; the retext and relabel are not readable); meanwhile artifact 17 lost clusterMovePropagatedToTheOtherSurface, so cross-surface propagation of a hand-driven cluster move is now claimed where it cannot be seen and seen where it is not claimed." },
    { "id": "04", "name": "Evidence and report integrity", "weight": 15, "minimum": 13, "score": 12,
      "reasoning": "Recapture discipline is excellent and independently verifiable: all 20 artifacts carry fresh fileMtimes inside this run's 04:20:13-04:35:24 window, capturedInThisRun true, staleFromEarlierCycles empty, every MANIFEST digest matches its file, and both external ledgers verify 28/28 with no unlisted files; substitutions are stamped into the pixels; the byte-identical 01/15 are explained by the frozen seed rather than hidden; every SSIM value in DIFF.json reproduces under my own gaussian-window implementation to within 0.020. But the diffing apparatus, which this category is scored by, has real holes: searchMatchReasonShown passes on artifact 10 while 83.7 percent of the chip it names is occluded (visible 47px of 288px) - a regression from a fully readable chip in cycle 9 - and DIFF records the same string as an onFrame value for artifact 04 where it is 91.3 percent hidden; DIFF does not diff claim sets at all, so eight artifacts changed claims and one lost a passing claim with no trace; whatChanged for all 10 recipe-changed artifacts cites a demonstrates string that is byte-identical to cycle 9's; and three artifacts (08, 18, 19) crossed their own SSIM thresholds unexplained with one outright classification inconsistency (19 subjectChanged true yet substantive false)." }
  ],
  "findings": [
    { "artifact": "10", "severity": "blocking", "subsystem": "evidence", "observed": "searchMatchReasonShown passes, but the 288px search chip (x1144-1432) is covered from x1191 by the labels-hidden chip; only '19 hit' is readable. Same defect on 04 (covered from x1169, 91.3% hidden), where DIFF records the full string as an onFrame value.", "rootCause": "When the node editor is open the right-aligned labels-hidden chip shifts left into the centred search chip; in 18_search_flyto.mp4 with the editor closed the two chips do not collide.", "correction": "Stack or re-row the chips when the editor narrows the toolbar, and add a claim that asserts the search reason's rendered extent is unoccluded, as artifact 12 already does for nodes." },
    { "artifact": "17", "severity": "blocking", "subsystem": "evidence", "observed": "clusterMovePropagatedToTheOtherSurface passed in cycle 9 and is absent in cycle 10, with no mention in DIFF.json. Eight artifacts changed claim sets; none appear in the diff.", "rootCause": "DIFF.json compares bytes, SSIM, recipe fingerprints, subject luminance and positions, but not the MANIFEST claim sets.", "correction": "Add a claims section to each DIFF row (added / removed / renamed) and fail the cycle on a removal with no stated replacement." },
    { "artifact": "12", "severity": "major", "subsystem": "sync", "observed": "clusterMoveCrossedTheBoundary is asserted, but no cluster appears anywhere in the 1920x2160 frame, and the dragged node did not cross the holding boundary (holding reads 4 in both 11 and 12, node outside the ring in both).", "rootCause": "A property of artifact 17 re-homed onto artifact 12 and reworded in the move, at the same cycle the corresponding 17 claim was deleted.", "correction": "Return the claim to artifact 17, whose frames do show a cluster move (11 of 134 markers displaced at t=23-24s while 123 hold to median 0.00px)." },
    { "artifact": "12", "severity": "major", "subsystem": "sync", "observed": "concurrentConflictKeptBoth asserted; the frame proves only the recolour (teal 18,90,84 -> magenta 141,42,92 at the transform-predicted position, residual 0.38px). The retext and relabel are unreadable: the label renders shortened as 'Demo: search...' and the editor is open on a different node.", "rootCause": "The editor was opened on the dragged node rather than the conflicted node.", "correction": "Open the editor on the conflicted node in one panel so its text and label fields carry the 'both kept' result." },
    { "artifact": "02", "severity": "major", "subsystem": "evidence", "observed": "All 10 recipe-changed artifacts are explained as 'capture script changed (hash -> hash); demonstrates now: X', and all 10 X strings are byte-identical to cycle 9's.", "rootCause": "whatChanged restates the current purpose instead of reporting the delta.", "correction": "Report 'demonstrates: unchanged' and name the on-frame counters that actually moved." },
    { "artifact": "19", "severity": "major", "subsystem": "evidence", "observed": "19 has subjectChanged true and ssim 0.9056 against a 0.93 threshold yet is substantive false with no whatChanged, while 12 with the same shape is substantive true with a note; 08 (0.968 vs 0.985) and 18 (0.928 vs 0.940) are below threshold with no explanation.", "rootCause": "The substantive rule is unstated and inconsistently applied.", "correction": "Always emit a notes entry when ssim falls below threshold, and derive substantive from one written rule." },
    { "artifact": "04", "severity": "major", "subsystem": "expansion", "observed": "Hidden labels rose 87 -> 105 and the 49-row unlabelled sidebar was removed (DIFF: unlabelledListed 50 -> 0) on the artifact defined as the whole map on screen at once and legible.", "rootCause": "The unlabelled enumeration was moved from 04 to 02 and the node editor took its place.", "correction": "Restore the enumeration alongside the editor, or write the trade-off up as a finding." },
    { "artifact": "11", "severity": "minor", "subsystem": "sync", "observed": "Markers are identical to the pixel, but 8 of 48 label runs differ between the Windows and Android panels of the same map at the same camera (116 hidden on both sides; 17 vs 19 shortened).", "rootCause": "The label arbiter is seeded or tie-broken per surface.", "correction": "Make label selection deterministic across surfaces, or state on-frame that it is per-surface." },
    { "artifact": "02", "severity": "minor", "subsystem": "evidence", "observed": "DIFF's unlabelledListed is one more than the frame renders: 48 vs 47 swatch rows in cycle-10 02, 50 vs 49 in cycle-9 04 (counted by connected components at x1630-1660, full height).", "rootCause": "A model count is reported under onFrameChanged.", "correction": "Count rendered rows, or rename the field." },
    { "artifact": "03", "severity": "minor", "subsystem": "ar", "observed": "The brief's definition asks for the holding cluster in frame; in the left panel only the ring's top arc and one truncated label are inside the device frame, with held markers clipped at the bottom edge and partly under the gyro HUD.", "rootCause": "The hero framing was narrowed to two orientations of the placed map.", "correction": "Widen or lower the vantage so the ring closes inside the frame, or declare the narrowing explicitly." }
  ],
  "comparisons": [
    { "artifact": "01", "verdict": "unchanged", "why": "Byte-identical to cycle 9 (sha 2c8ae64e), explained by the frozen seed epoch and corroborated by a fresh fileMtime inside this run." },
    { "artifact": "02", "verdict": "improved", "why": "Label overlaps eliminated, shortened 36 -> 16, and the 109 unlabelled thoughts now enumerated; all markers within 1px of cycle 9." },
    { "artifact": "03", "verdict": "unchanged", "why": "Same two-orientation hero; the declared 271px travel reconstructs to 268.5 device px from measured pixels. Holding cluster still clipped." },
    { "artifact": "04", "verdict": "regressed", "why": "Search-breakdown chip born 91.3% occluded; unlabelled enumeration dropped 49 -> 0; hidden labels 87 -> 105." },
    { "artifact": "05", "verdict": "unchanged", "why": "Synthetic-source stamp retained; before/after is a pure dolly (uniform scale 1.136 over 27 inlier markers, residual <=1.96px)." },
    { "artifact": "06", "verdict": "improved", "why": "Reframed onto the holding ring; 8 held markers countable inside it, count chip agrees, 8 displaced labels enumerated." },
    { "artifact": "07", "verdict": "unchanged", "why": "ssim 0.987; all 8 markers within 1px; search-hit four-tick signature verified at pixel level." },
    { "artifact": "08", "verdict": "unchanged", "why": "ssim 0.968 (below its 0.985 threshold, unexplained by DIFF) but the substance holds: 8 of 10 markers within 1px across the frozen camera, only the placed node moved, holding 4 -> 3." },
    { "artifact": "09", "verdict": "unchanged", "why": "ssim 0.989; gained everyOtherLabelHeldItsPlace." },
    { "artifact": "10", "verdict": "regressed", "why": "Search match reason went from fully readable to 83.7% occluded while its claim still passes." },
    { "artifact": "11", "verdict": "unchanged", "why": "ssim 0.9997; canvas regions of the two panels differ by exactly zero." },
    { "artifact": "12", "verdict": "unchanged", "why": "ssim 0.813 reflects a reframe, not a position change: all 49 bottom-row markers identical across panels, and 11->12 is one uniform similarity with a single node moved by the declared drag. New claim clusterMoveCrossedTheBoundary is uncorroborated." },
    { "artifact": "13", "verdict": "unchanged", "why": "ssim 0.998." },
    { "artifact": "14", "verdict": "improved", "why": "detailIsMagnified renamed to detailExceedsPanelScale, correcting a name that did not match what was checked (three of four detail panels are below 1x of app pixels in both cycles)." },
    { "artifact": "15", "verdict": "unchanged", "why": "Byte-identical to cycles 8 and 9; the 6 touch rows and 4 pose rows match the captions in videos 16 and 17." },
    { "artifact": "16", "verdict": "unchanged", "why": "ssim 0.976; 7 labelled gestures (Tap, Double-tap, Long-press, Drag a node, Drag empty space, Pinch/spread, Gyroscope), 34s/30fps/1920x1080; no drift (max 0.13px over 3s)." },
    { "artifact": "17", "verdict": "regressed", "why": "Lost the passing claim clusterMovePropagatedToTheOtherSurface with no replacement and no mention in DIFF; the take remains single-surface." },
    { "artifact": "18", "verdict": "improved", "why": "ssim 0.928; the same label rework, and the search chip reads in full here because the editor is closed." },
    { "artifact": "19", "verdict": "improved", "why": "ssim 0.906 with subjectChanged true but classified non-substantive and unexplained; the visible change is the label rework, and the placed node lands at the same 21.9, -14.6, 13.7 as cycle 9." },
    { "artifact": "20", "verdict": "unchanged", "why": "ssim 0.9995; no node drift over t=5-8s (max 0.14px across 11 markers)." }
  ],
  "verdict": "Cycle 10's central promise is the best-evidenced thing in the set: node positions are proven identical across surfaces and across cycles at the pixel, not asserted - zero difference across artifact 11's twin canvases, 49 identical marker centroids in artifact 12's 150-node row, a single uniform similarity from 11 to 12 with exactly one node moved by the declared drag, artifact 03's stated 271px travel reconstructing to 268.5 device px from measured pixels, and no idle drift above 0.14px in any video window I sampled. The evidence apparatus is genuinely auditable - both ledgers verify 28/28, every manifest digest matches its file, the previous cycle's count is taken from files rather than its own manifest, and every SSIM in DIFF.json reproduces independently. But the cycle is not regression-free: the search match reason regressed from readable to 84 percent occluded on artifact 10 while the claim asserting it still passes, artifact 04 shipped the same chip born occluded and lost its unlabelled enumeration, and artifact 17 quietly lost a passing cross-surface claim that the diff cannot see because it does not diff claims at all. Category 03 clears its gate at 17; category 04 does not, at 12 - the recapture discipline is exemplary but the diff has a blind spot in exactly the mechanism the category rests on, and one machine-checked claim is contradicted by its own frame."
}
```