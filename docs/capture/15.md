# 15 · 15_settings_gestures.png

**Settings and gesture reference**

| | |
|---|---|
| Surface | windows |
| Map | map-fermentation |
| Camera clip | harness/clips/hand-vocabulary.mp4 through Chromium's fake capture device |
| Minimum | 1920x1080 |
| Driver | `harness/drivers/stills.mjs`, entry `15` |
| Run | `node harness/run-capture.mjs --only 15` |

## State

Settings open with hand tracking switched on and tracking live.

## Vantage

Full-screen settings overlay, scrolled to top.

## What it proves

The settings region: hand-tracking toggle with live status, account sign-in, sync status, and the in-app reference listing every touch and hand gesture with its operation.

## Determinism

The sync service is started with a freshly wiped data directory seeded from the
committed fixtures in `seeds/`. Before the app boots, the harness pins
`Date.now()` to a fixed epoch plus elapsed time and replaces `Math.random` with
a seeded PRNG (`harness/capture.mjs`, `SEED`), so generated node ids and
relative times are identical from cycle to cycle. The camera pose is set
explicitly rather than left to whatever the previous interaction left behind.
